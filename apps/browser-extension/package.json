{
  "name": "context-layer-browser-extension",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check service-worker.js && node --check protocol.js",
    "test": "node --test tests/*.test.mjs"
  }
}
